import { types } from "../types/types";


export const uiOpenModalStoreAction = () => ( {
  type: types.uiOpenModalAction
} );

export const uiCloseModalStoreAction = () => ( {
  type: types.uiCloseModalAction
} );
